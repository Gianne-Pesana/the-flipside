import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  devIndicators: {
    position: "bottom-right", // default is top-right; can also be top-left, bottom-left, bottom-right
  },
};

export default nextConfig;